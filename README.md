# Portfolio Analytics

Static GitHub Pages app. Market prices are fetched at build time into `public/data/prices.json`. Personal brokerage CSV exports stay local (gitignored) and are uploaded only in the browser.

```bash
npm install
npm run data
npm run dev
npm run build
```

Site path: `/stocks/` (GitHub Pages).
